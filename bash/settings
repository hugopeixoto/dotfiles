alias grep="grep --color --exclude=\*.un~"
alias fgrep="fgrep --color --exclude=\*.un~"

export EDITOR=vim
export MAKEFLAGS="-j $(nproc)"

export PS1='${debian_chroot:+($debian_chroot)}\u@\h:$(echo "${PWD/$HOME/\~}" | tico)\$ '
